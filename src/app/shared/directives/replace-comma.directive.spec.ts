import { ReplaceCommaDirective } from './replace-comma.directive';

describe('ReplaceCommaDirective', () => {
  it('should create an instance', () => {
    // const directive = new ReplaceCommaDirective();
    // expect(directive).toBeTruthy();
  });
});
